<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Night-Out" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

<nav class="top-bar fixed" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "Night-Out" %></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <li class="active"><a href="#"></a></li>
      <li class="has-dropdown">
<% if user_signed_in? %>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
<% end %>
        <ul class="dropdown">
          <li><a href="#"></a></li>
        </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
  <% if user_signed_in? %>
      <ul class="left">
            <li><%= link_to "ALL GROUPS", root_path %></li>
            <% @your_groups.each do |group| %>
                  <li><%= link_to "#{group.name}", group_path(group) %></li>
              <% end %>
        </ul>
  <% end %>

  </section>
</nav>


    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "#{name} alert-box" %>
    <% end %>

    <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>